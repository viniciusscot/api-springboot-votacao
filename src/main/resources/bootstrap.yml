spring:
  application:
    name: api-springboot-votacao
  cloud:
    config:
      enabled: false

server:
  servlet:
    context-path: /${spring.application.name}

logging.level:
  root: ERROR
  com.sicredi.votacao: INFO